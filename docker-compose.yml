name: nuxt-4-site-frontend
services:
  # Production environment
  app-production:
    profiles: [ "production" ]
    #    env_file: .env.local.production
    build:
      context: .
      dockerfile: .docker/Dockerfile
      args:
        NODE_ENV: production
    container_name: nuxt-4-front-production
    restart: unless-stopped
    ports:
      - '8082:3001'

  # Staging environment
  app-staging:
    profiles: [ "staging" ]
    #    env_file: .env.local.staging
    build:
      context: .
      dockerfile: .docker/Dockerfile
      args:
        NODE_ENV: staging
    container_name: nuxt-4-front-staging
    restart: unless-stopped
    ports:
      - '8082:3001'

  # Development environment
  app-development:
    profiles: [ "development" ]
    #    env_file: .env.local.development
    build:
      context: .
      dockerfile: .docker/Dockerfile
      args:
        NODE_ENV: development
    container_name: nuxt-4-front-development
    restart: unless-stopped
    ports:
      - '8082:3001'

  # Local development with hot-reload
  app-local:
    profiles: [ "local" ]
    env_file: .env.local
    build:
      context: .
      dockerfile: .docker/Dockerfile
      args:
        NODE_ENV: development
      target: local
    container_name: nuxt-4-front-local
    restart: always
    volumes:
      - ./:/var/www/site-dev/
      - /var/www/site-dev/.nuxt
      - /var/www/site-dev/node_modules
    ports:
      - '8006:5173'
      - '24679:24678'
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.app-local-external.rule=Host(`nuxt-4-site-frontend.localhost`) || HostRegexp(`[a-z0-9-]+.nuxt-4-site-frontend.localhost`)"
      - "traefik.http.routers.app-local-external.entrypoints=web"
      - "traefik.http.services.app-local.loadbalancer.server.port=5173"
      - "traefik.docker.network=shared_network"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - shared_network

networks:
  shared_network:
    external: true
